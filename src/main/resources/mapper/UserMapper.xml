<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.tommyadmin.common.security.mapper.UserMapper">
    <resultMap id="BaseUserResultMap" type="io.github.tommyadmin.common.security.entity.User">
        <id column="id" property="id"/>
        <result column="user_name" property="userName"/>
        <result column="password" property="password"/>
        <result column="nick_name" property="nickName"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="is_active" property="active"/>
    </resultMap>

    <sql id="Base_Column_List">
    id, user_name, `password`, nick_name, phone, email, is_active
    </sql>
    <select id="findOneWithAuthoritiesByUsername" resultMap="BaseUserResultMap">
        SELECT
        u.id,u.password,m.permission
        FROM
        sys_user u
        LEFT JOIN sys_user_role ur ON u.id = ur.user_id
        LEFT JOIN sys_role r ON r.id = ur.role_id
        LEFT JOIN sys_role_menu rm ON r.id = rm.role_id
        LEFT JOIN sys_menu m ON rm.menu_id = m.id
        WHERE
        u.user_name = #{username}
    </select>

</mapper>
